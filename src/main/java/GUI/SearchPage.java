package main.java.GUI;

import javax.swing.*;
import java.awt.event.ActionListener;
import java.util.ArrayList;
import java.awt.*;
import java.awt.event.ActionEvent;


public class SearchPage {
    private JList list1;

    public SearchPage() {
        /*§JFrame frame = new JFrame("Lista Dinamica");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        // Creazione di una lista dinamica di elementi
        ArrayList<String> elementi = new ArrayList<>();

        // Creazione del modello per la JList
        DefaultListModel<String> listModel = new DefaultListModel<>();
        JList<String> jList = new JList<>(listModel);

        // Creazione di un JScrollPane per la JList
        JScrollPane scrollPane = new JScrollPane(jList);

        // Creazione di un pulsante per aggiungere elementi alla lista
        JButton addButton = new JButton("Aggiungi Elemento");
        addButton.addActionListener(new ActionListener() {
            @Override
            public void actionPerformed(ActionEvent e) {
                // Chiedi all'utente di inserire un elemento
                String nuovoElemento = JOptionPane.showInputDialog(frame, "Inserisci un nuovo elemento:");

                // Aggiungi l'elemento alla lista
                if (nuovoElemento != null && !nuovoElemento.isEmpty()) {
                    elementi.add(nuovoElemento);
                    listModel.addElement(nuovoElemento);
                }
            }
        });

        // Creazione di un pannello per organizzare i componenti
        JPanel panel = new JPanel(new BorderLayout());
        panel.add(scrollPane, BorderLayout.CENTER);
        panel.add(addButton, BorderLayout.SOUTH);

        frame.add(panel);
        frame.setSize(300, 200);
        frame.setVisible(true);
    }*/
    }
}
